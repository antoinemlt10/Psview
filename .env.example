# Required ONLY once the real reasoning engine replaces the stub in lib/stub-agent.ts.
# The stub (lib/stub-agent.ts) needs no key — the app is fully demo-able without it.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
