# Required: the reasoning engine (lib/agent/*) calls the Anthropic API server-side.
# Read only in lib/agent/llm.ts — never exposed to the client. Copy to .env.local.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
